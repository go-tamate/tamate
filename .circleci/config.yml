version: 2
jobs:
  build:
    working_directory: /go/src/github.com/Mitu217/tamate
    docker:
      - image: circleci/golang:1.9
        environment:
          - REVIEWDOG_VERSION: 0.9.8
          - TAMATE_MYSQL_DSN: "root:example@tcp(localhost:3306)/"
      - image: circleci/mysql:5.7
        environment:
          - MYSQL_ROOT_PASSWORD: example
    steps:
      - checkout
      - run: mkdir -p ~/bin/
      - run: curl -fSL https://github.com/haya14busa/reviewdog/releases/download/$REVIEWDOG_VERSION/reviewdog_linux_amd64 -o ~/bin/reviewdog && chmod +x ~/bin/reviewdog
      - run: go get -u github.com/golang/lint/golint
      - run: go get -u honnef.co/go/tools/cmd/megacheck
      - run: go get -u github.com/kisielk/errcheck
      - run: ~/bin/reviewdog -ci="circle-ci" -conf=.reviewdog.yml
      - restore_cache:
          key: gopkg-{{ .Branch }}-{{ checksum "Gopkg.lock" }}
          paths:
            - /go/src/github.com/Mitu217/tamate/vendor
      - run:
          name: Download libraries
          command: |
            if [ ! -d /go/src/github.com/Mitu217/tamate/vendor ]; then
              go get -u github.com/golang/dep/cmd/dep
              dep ensure -v
            fi
      - save_cache:
          key: gopkg-{{ .Branch }}-{{ checksum "Gopkg.lock" }}
          paths:
            - /go/src/github.com/Mitu217/tamate/vendor
      - run:
          name: Wait for db
          command: dockerize -wait tcp://localhost:3306 -timeout 1m
      - run: go test -v -race ./...
